# Миграции Supabase

Каждое изменение схемы добавляйте новым SQL-файлом с временной меткой в этой папке.

Правило: таблица и её RLS-политики должны появляться в одной миграции. Применяйте миграции через Supabase CLI:

```bash
npx supabase db push
```
